---
layout: layouts/base.njk
title: Inicio
description: Salsas artesanales, snacks botánicos y despensa de bienestar creados entre Florida y Costa Rica.
locale: es
localeLinks:
  en: /
---

{% set translations = collections.i18n and collections.i18n[0].data.i18n %}
{% set localeStrings = translations and translations.es %}
{% if localeStrings == null and translations and translations['en'] %}
  {% set localeStrings = translations['en'] %}
{% endif %}
{% set t = localeStrings or {} %}
{% set heroStrings = t.hero or {} %}
{% set visionStrings = t.vision or {} %}
{% set newsletterStrings = t.newsletter or {} %}
{% set catalogData = catalog.catalog %}
{% if catalogData == null %}
  {% set catalogData = catalog %}
{% endif %}
{% set categories = catalogData.categories %}

<section class="hero">
  <div class="hero-media">
    <picture>
      <source srcset="{{ '/img/branding/creaciones-colibri-logo.svg' | url }}" type="image/svg+xml">
      <img class="hero-logo" src="{{ '/img/branding/creaciones-colibri-logo.svg' | url }}" alt="Logotipo de Colibrí">
    </picture>
  </div>
  <div class="hero-content">
    <h1>{{ heroStrings.title or 'Calor botánico y despensa vibrante.' }}</h1>
    <p>{{ heroStrings.intro or 'Creaciones Colibrí es un estudio culinario artesanal que combina ingredientes tropicales, chiles audaces y prácticas regenerativas en salsas, snacks y bebidas que nutren el cuerpo y la historia detrás de cada frasco.' }}</p>
    <a class="button button-primary" href="{{ '/es/productos/' | url }}">{{ heroStrings.cta or 'Descubre los sabores' }}</a>
  </div>
</section>

<section id="vision" class="content-section">
  <h2>{{ visionStrings.title or 'Enraizados en la historia y la sostenibilidad' }}</h2>
  <p>{{ visionStrings.intro or 'Desde nuestros huertos en Florida y Costa Rica honramos la biodiversidad centroamericana mientras cuidamos la tierra. Espera empaques compostables, ingredientes de comercio justo y mucho storytelling culinario.' }}</p>
</section>

<section id="catalogo" class="content-section content-grid">
  {% if categories and categories | length > 0 %}
    {% for category in categories %}
      {% set categoryAnchor = category.slug | default(category.id) %}
      {% set firstProduct = category.products | first %}
      {% set categoryLink = '/es/productos/#categoria-' ~ categoryAnchor %}
      <article class="card">
        <h3>{{ category.name }}</h3>
        {% if firstProduct %}
          <p>{{ firstProduct.shortDescription }}</p>
        {% endif %}
        <a class="button button-secondary" href="{{ categoryLink | url }}">Ver {{ category.name | lower }}</a>
      </article>
    {% endfor %}
  {% endif %}
</section>

<section id="newsletter" class="content-section newsletter">
  <h2>{{ newsletterStrings.title or 'Permanece al tanto' }}</h2>
  <p>{{ newsletterStrings.intro or 'Estamos desarrollando nuevas recetas, recopilando historias de nuestros aliados y preparando el lanzamiento. Deja tu correo para enterarte primero.' }}</p>
  {% if site.forms.newsletterAction %}
    <form
      class="newsletter-form"
      action="{{ site.forms.newsletterAction }}"
      method="POST"
      data-analytics-event="newsletter_signup"
      data-analytics-form="newsletter"
    >
      <label class="newsletter-form__label" for="newsletter-email-es">{{ newsletterStrings.label or 'Correo electrónico' }}</label>
      <div class="newsletter-form__controls">
        <input id="newsletter-email-es" class="newsletter-form__input" type="email" name="email" placeholder="{{ newsletterStrings.placeholderInput or 'tú@ejemplo.com' }}" autocomplete="email" required>
        <button class="button button-primary newsletter-form__submit" type="submit">{{ newsletterStrings.cta or 'Avisarme' }}</button>
      </div>
    </form>
  {% else %}
    {% set placeholderCopy = newsletterStrings.placeholder or 'Pronto activaremos nuestro boletín. Por ahora escríbenos a <a href="mailto:hola@creacionescolibri.com">hola@creacionescolibri.com</a>.' %}
    <p class="newsletter-placeholder">{{ placeholderCopy | safe }}</p>
  {% endif %}
</section>
